pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t vijjineni/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag vijjineni/request-logger:${env.BUILD_ID} vijjineni/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push vijjineni/request-logger:${env.BUILD_ID}"
        sh "docker push vijjineni/request-logger:latest"
      }
    }
  }
}
